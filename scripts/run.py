"""
run.py — DEPRECATED

This file is no longer the entry point for seed-demo-data.

The wizard lives in SKILL.md and runs through Claude Code:

    1. Open Claude Code in any directory
    2. Type:  /seed-demo-data
       or say: "Seed a Data Cloud demo for <client name>"

The wizard handles everything: org connection, industry detection,
data generation, upload, DMOs, mappings, IR, CIs, and segments.

See INSTALL.md for setup instructions.
"""

print(
    "\n"
    "  seed-demo-data is now a Claude Code skill.\n"
    "\n"
    "  Open Claude Code and type:\n"
    "    /seed-demo-data\n"
    "\n"
    "  See INSTALL.md for full setup instructions.\n"
)
